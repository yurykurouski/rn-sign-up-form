import { RED } from 'assets';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  margin20: {
    marginTop: 20,
  },
  errMsg: {
    marginTop: 6,
    color: RED,
    fontFamily: 'Inter',
    fontWeight: '500',
  },
});
