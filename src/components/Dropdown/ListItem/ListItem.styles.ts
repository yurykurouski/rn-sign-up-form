import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
  },
  image: {
    width: 20,
    height: 14,
    marginRight: 10,
  },
  name: {
    fontFamily: 'Inter',
    fontSize: 18,
  },
});
