import { GRAY_900 } from 'assets';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: GRAY_900,
    padding: 12,
    borderRadius: 8,
    alignItems: 'center',
  },
  label: {
    color: 'white',
    fontFamily: 'Inter',
    fontWeight: 'bold',
    fontSize: 16,
  },
});
